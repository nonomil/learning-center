# 学习中心

这是从幼小衔接到小学阶段的独立学习项目，不是主站 `宠物积分系统` 的运行时子目录。

## 本地运行

```powershell
npm run serve
```

默认地址：`http://127.0.0.1:7001/`

学习进度使用 `learncenter_*` 命名空间保存。由主站新窗口启动时，URL hash 可携带 `petbankLaunch` 和 `petbankProfile`；完成学习单元后只回传完成事件，主站决定是否发放成长奖励。

生产发布地址：`https://nonomil.github.io/learning-center/`
